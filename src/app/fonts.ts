import { Readex_Pro } from "next/font/google";
import localFont from "next/font/local";

export const excon = localFont({
  src: "./Excon-Variable.woff2",
  display: "swap",
});
export const readex = Readex_Pro({ subsets: ["latin"] });
